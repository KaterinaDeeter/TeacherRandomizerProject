package TeacherSelection;

public class TeacherSelect
{
    //This should try reading SchoolOptions.txt. Look for 'The School Is' and set school to that option
    //Once it receives a school option, then we can make it access the "school database". Once it accesses school
    //then it can select a grade (randomizer, done at some point), then teacher...
    //Or just a dropdown for each school but that will be more user work. .equalsIgnoreSpaces would probably be helpful
}
